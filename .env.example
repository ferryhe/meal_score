# Example .env file
#
# IMPORTANT: Make a copy of this file and name it .env
# You can then fill in your actual database credentials in that file.
#
# The .env file is ignored by git, so your credentials will not be
# checked into version control.

# Docker/Postgres settings (required for docker-compose)
POSTGRES_USER=meal_score
POSTGRES_PASSWORD=change_me
POSTGRES_DB=meal_score

# Connection string for your PostgreSQL database
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
DATABASE_URL=postgresql://meal_score:change_me@db:5432/meal_score
